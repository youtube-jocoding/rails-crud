<div class="container mt-5">
	<h1 class="text-center">CRUD 연습 게시판</h1>
	<form class="mt-3 mb-5" action="/create" method="post">
		<input name="authenticity_token" value="<%= form_authenticity_token %>" type="hidden">
		<div class="form-group">
			<label for="exampleInputEmail1">제목</label>
			<input type="text" class="form-control" name="title" class="form-control" id="exampleInputEmail1" aria-describedby="emailHelp" placeholder="제목을 입력하세요...">
		</div>
		<div class="form-group">
    <label for="exampleFormControlTextarea1">내용</label>
    <textarea name="content" class="form-control" id="exampleFormControlTextarea1" rows="3" placeholder="내용을 입력하세요..."></textarea>
  	</div>
		<button type="submit" class="btn btn-primary">저장하기</button>
	</form>
	<% @posts.each do |post| %>
	<div class="card mt-1">
		<div class="card-body">
			<h5 class="card-title">ID: <%= post.id %></h5>
			<h6 class="card-subtitle mb-2">제목: <%= post.title %></h6>
			<p class="card-text">내용: <%= post.content %></p>
			<a href="/edit?id=<%= post.id %>" class="btn btn-primary">수정하기</a>
			<a href="/delete?id=<%= post.id %>" class="btn btn-danger">삭제하기</a>
		</div>
	</div>
	<% end %>
</div>